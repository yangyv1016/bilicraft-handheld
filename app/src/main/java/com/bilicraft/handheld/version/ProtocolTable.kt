package com.bilicraft.handheld.version

/**
 * 内置协议号映射表（离线核心资产）。
 *
 * 数据来源：wiki.vg / minecraft.wiki 协议版本页。
 * 只收录「协议号发生变化的代表版本」——同一协议号下的小版本共享同一条目，
 * 因为握手只认协议号，不认具体版本名。
 *
 * 覆盖策略（对齐用户确认的「聊天核心优先」）：
 * 近代版本（1.19+）协议号精确；老版本给出代表性锚点，其余靠自动识别兜底。
 */
object ProtocolTable {

    /** id → protocol number。id 用该协议号的代表版本。 */
    val entries: List<McVersion> = listOf(
        // ---- 26.x（Mojang 2026 起改用「年.次」版本号方案）----
        rel("26.2", 776),
        rel("26.1", 775),
        // ---- 1.21.x ----
        rel("1.21.11", 774),
        rel("1.21.9", 773),
        rel("1.21.8", 772),
        rel("1.21.6", 771),
        rel("1.21.5", 770),
        rel("1.21.4", 769),
        rel("1.21.2", 768),
        rel("1.21", 767),
        // ---- 1.20.x ----
        rel("1.20.5", 766),
        rel("1.20.3", 765),
        rel("1.20.2", 764),
        rel("1.20", 763),
        // ---- 1.19.x ----
        rel("1.19.4", 762),
        rel("1.19.3", 761),
        rel("1.19.1", 760),
        rel("1.19", 759),
        // ---- 1.18.x ----
        rel("1.18.2", 758),
        rel("1.18", 757),
        // ---- 1.17.x ----
        rel("1.17.1", 756),
        rel("1.17", 755),
        // ---- 1.16.x ----
        rel("1.16.5", 754),
        rel("1.16.4", 754),
        rel("1.16.2", 751),
        rel("1.16.1", 736),
        rel("1.16", 735),
        // ---- 1.15.x ----
        rel("1.15.2", 578),
        rel("1.15", 573),
        // ---- 1.14.x ----
        rel("1.14.4", 498),
        rel("1.14", 477),
        // ---- 1.13.x ----
        rel("1.13.2", 404),
        rel("1.13", 393),
        // ---- 1.12.x ----
        rel("1.12.2", 340),
        rel("1.12", 335),
        // ---- 1.11.x ----
        rel("1.11.2", 316),
        rel("1.11", 315),
        // ---- 1.10.x ----
        rel("1.10.2", 210),
        // ---- 1.9.x ----
        rel("1.9.4", 110),
        rel("1.9", 107),
        // ---- 1.8.x ----
        rel("1.8.9", 47),
        // ---- 1.7.x ----
        rel("1.7.10", 5),
        rel("1.7.2", 4),
    )

    /** 已知版本 id → 协议号，供协议层快速查表 */
    val byId: Map<String, Int> =
        entries.mapNotNull { v -> v.protocolNumber?.let { v.id to it } }.toMap()

    /** 最新正式版（下拉框置顶用） */
    val latestRelease: McVersion = entries.first()

    private fun rel(id: String, proto: Int) =
        McVersion(id = id, type = VersionType.RELEASE, protocolNumber = proto)
}