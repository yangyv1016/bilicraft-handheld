## 掌上碧玺 v1.1.0

本次更新新增离线登录，方便连接关闭正版验证的 Minecraft 服务器：

- 登录页支持使用玩家名创建离线账号，无需微软授权
- 使用 Minecraft 标准离线 UUID，重启应用后仍可继续使用
- 账号列表会区分微软账号与离线账号，并支持切换和移除
- 离线账号不会请求 Token 或 Mojang 聊天签名证书
- 尝试进入开启正版验证的服务器时会明确提示，不再产生模糊的会话校验错误

请注意：离线账号只能进入关闭正版验证（`online-mode=false`）的服务器。
